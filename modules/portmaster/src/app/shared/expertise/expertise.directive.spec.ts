import { ExpertiseDirective } from './expertise.directive';

describe('ExpertiseDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpertiseDirective();
    expect(directive).toBeTruthy();
  });
});
